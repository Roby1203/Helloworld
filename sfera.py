import math
 
raggio = input("inserisci il raggio:"   )
superfice = 4. * math.pi  * raggio * raggio
volume =( 4. / 3.)  * math.pi  * raggio * raggio * raggio

print "la sfera ha una superficie di ", superfice, " e un volume di", volume 
